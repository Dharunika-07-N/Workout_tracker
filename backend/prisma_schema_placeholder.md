This project will use PostgreSQL and Prisma (or SQLAlchemy if using Python).

Create `prisma/schema.prisma` later with models matching `PRD.doc` (Users, UserProfiles, Equipment, Exercises, WorkoutSessions, WorkoutExercises, HealthFeedback, UserGoals, ML_Recommendations).

For now this placeholder indicates where the schema will live.
