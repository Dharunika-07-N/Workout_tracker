# Workout Tracker — Monorepo

This workspace contains a minimal scaffold for the AI-powered workout tracker project described in `PRD.doc`.

Structure:

- backend/ — minimal Express API
- frontend/ — minimal Vite + React scaffold

What I added in this step:

- README.md (this file)
- .gitignore
- .env.example
- backend/server.js and package.json
- frontend Vite scaffold files and package.json

Next steps:

- Run `npm install` in `backend` and `frontend` and start dev servers
- Implement DB schema and authentication
